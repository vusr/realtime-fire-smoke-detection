# Core dependencies for fire detection inference
# Python 3.10+

# PyTorch (pre-installed in base image, but specify versions if needed)
torch>=2.10.0
torchvision>=0.25.0

# YOLO and object detection
ultralytics>=8.4.0

# TensorRT inference (usually comes with NVIDIA base images)
# tensorrt>=10.15.0

# CUDA acceleration for TensorRT
# pycuda>=2024.1  # Often incompatible, may need specific version

# Computer vision
opencv-python>=4.13.0

# Numerical computing
numpy>=2.3.0,<3.0.0  # Be careful with numpy 2.x compatibility

# Progress bars
tqdm>=4.67.0

# Data handling
pandas>=2.0.0

# Image processing
Pillow>=12.0.0

# YAML parsing (for config files)
PyYAML>=6.0

# Performance monitoring
psutil>=7.2.0

# Optional: Visualization
matplotlib>=3.10.0

# TensorFlow/Keras (if you used it for evaluation)
tensorflow>=2.20.0

# Protobuf (important for TensorRT)
protobuf>=7.34.0

# Additional dependencies from your VM
flatbuffers>=25.12.0
